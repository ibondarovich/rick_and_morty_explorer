name: data
description: "A new Flutter package project."
publish_to: none

environment:
  sdk: ^3.7.2
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core

  domain:
    path: ../domain
  json_annotation: ^4.9.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8
  json_serializable: ^6.8.0
  flutter_lints: ^3.0.0